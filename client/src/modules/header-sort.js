export default function sortData () {
    const name = document.getElementById('sort-name');
    const mode = document.getElementById('sort-mode');
    const place = document.getElementById('sort-place');
    const startDate = document.getElementById('sort-startDate');
    const lastRegDate = document.getElementById('sort-lastRegDate');
    const state = document.getElementById('sort-state');
    const level = document.getElementById('sort-level');
    const participants = document.getElementById('sort-participants');
    const scenario = document.getElementById('sort-scenario');
    const actions = document.getElementById('sort-actions');

    name.addEventListener('click' , () => {
        console.log('ki')
    })
}